/*
     MAIN.CC
*/

// This function gets points from a user and adds
// the points to a vector which is then returned
vector<Point> generatePointsVector();

// This function takes a vector of points and uses
// those points to calculate a polygonal area
void calculateArea(vector<Point>);
